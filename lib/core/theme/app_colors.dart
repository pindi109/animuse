
import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Backgrounds
  static const Color background = Color(0xFFFFFFFF);
  static const Color surface = Color(0xFFF8F8F7);
  static const Color border = Color(0xFFE8E8E4);

  // Text
  static const Color textPrimary = Color(0xFF111111);
  static const Color textSecondary = Color(0xFF666666);

  // Accent — warm coral/salmon
  static const Color accent = Color(0xFFE8593C);
  static const Color accentHover = Color(0xFFD04828);
  static const Color accentLight = Color(0xFFFAECE7);

  // Status
  static const Color statusPending = Color(0xFFE8E8E4);
  static const Color statusGenerating = Color(0xFFFAEEDA);
  static const Color statusReady = Color(0xFFEAF3DE);
  static const Color statusApproved = Color(0xFFE1F5EE);
  static const Color statusFailed = Color(0xFFFCEBEB);
  static const Color statusLocked = Color(0xFFEEEDFE);
}